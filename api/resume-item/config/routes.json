{
  "routes": [
    {
      "method": "GET",
      "path": "/resume-items",
      "handler": "resume-item.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/resume-items/count",
      "handler": "resume-item.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/resume-items/:id",
      "handler": "resume-item.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/resume-items",
      "handler": "resume-item.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/resume-items/:id",
      "handler": "resume-item.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/resume-items/:id",
      "handler": "resume-item.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
